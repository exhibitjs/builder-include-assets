{
  "name": "exhibit-include-assets",
  "version": "0.0.2",
  "author": "Callum Locke",
  "devDependencies": {
    "babel": "^5.5.8",
    "eslint": "^0.20.0",
    "nodemon": "^1.3.7"
  },
  "files": [
    "dist",
    "src"
  ],
  "keywords": [
    "exhibit-plugin"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "prepublish": "npm run build",
    "watch": "nodemon --exec 'npm run build' --watch src --watch index.js --watch package.json"
  },
  "dependencies": {
    "find-assets": "^0.3.1"
  }
}
